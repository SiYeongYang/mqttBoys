# 성능 검증

## v0.0.15, 2026-09-05

동일 PC의 .NET 10.0.400, Windows x64, Release 구성에서 이전 버전(v0.0.14)과 개선본을 비교했습니다.

### Selected 재선택

61,500자 합성 JSON(800개 항목)을 한 번 선택한 뒤, 같은 메시지의 History 행을 100번 다시 연결했습니다. 이는 실시간 History 목록을 갱신하면서 Selected를 복원하는 작업에 해당합니다. 5회 준비 실행 후 측정했습니다.

| 측정값 | v0.0.14 | v0.0.15 개선본 |
| --- | ---: | ---: |
| 100회 처리 시간 | 373.24ms | 0.37ms |
| 해당 스레드의 관리 힙 할당량 | 186,241,192 bytes | 125,992 bytes |

시간은 `Stopwatch`, 할당량은 `GC.GetAllocatedBytesForCurrentThread`로 측정했습니다. 같은 메시지를 다시 포맷하지 않으면서 사라진 작업의 비용을 보여주는 국소 측정입니다. 전체 프로세스의 RAM 사용량, MQTT 수신 처리량, 장기 운영 결과를 의미하지 않습니다.

### 회귀 및 UI 검증 범위

- 수신이 멈춘 뒤에도 묶음의 마지막 메시지가 Value와 History에 반영되는지 확인했습니다.
- 더 새 메시지가 수신되어도 완료된 포맷 결과가 표시되고, 토픽 변경 전 결과는 적용되지 않는지 확인했습니다.
- 선택 메시지의 보관 및 ASCII 전환, 줄바꿈, 짧은 문서의 가로 스크롤, 검색 입력창과 본문 분리, 스크롤 위치 보존을 확인했습니다.
- WPF 화면을 1920x1080, 1440x900, 1366x768, 1100x720 크기로 오프스크린 렌더링해 주요 영역의 경계와 배치를 확인했습니다.
- 실제 운영 브로커와 장시간 연결하는 부하 시험은 수행하지 않았습니다.
